---
类型: 指南
状态: 有效
更新日期: 2026-07-26
适用版本: 脚本 0.3.5
---

# 待办事项

从历次重构跟进中提取的、当前仍然有效的技术债与后续工作。已完成的条目请直接删除，不要留 `~~删除线~~`。

## 测试覆盖

- [ ] 为 pricing query 字段补充逻辑（现位于 `src/graphql.js`）添加针对性测试：
  - 已有 `mode` 但缺少 `flatFee`
  - 已有 `flatFee` 但缺少 `mode`
  - 多个 `pricing` selection
  - 嵌套的 pricing 形状块
  - fragment 形式的模型详情 query
- [ ] 补充 fetch body 回归覆盖（`src/fetch-patch.js`）：
  - `fetch(url, { body })`
  - `fetch(new Request(url, { body }))`
  - pricing 字段注入保持 method、headers、body 不变
  - 拦截处理后不出现 body 已消费错误

## 源码结构

- [ ] 以小步、行为不变的方式继续拆分 `src/fetch-patch.js`：
  - 请求/请求头上下文 helper
  - 响应包装 helper
  - 资源市场渠道 URL 清理 helper

## 构建

- [ ] ESM / esbuild 迁移准备，路线见[开发指南](开发指南.md#未来-esm--esbuild-迁移路径)。

## 发布前冒烟

每次发布前在真实登录会话中验证，记录浏览器、账号状态与 console/network 报错：

- [ ] 资源市场：全部/免费筛选正常，渠道卡片可用
- [ ] 模型详情页：pricing 字段补充后价格数据正常加载
- [ ] 密钥页：弹窗开关、配置绑定、渠道列表编辑与保存正常
- [ ] 请求页：入口挂载、请求触发控件、渠道映射显示正常
