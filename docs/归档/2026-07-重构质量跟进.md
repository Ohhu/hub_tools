---
类型: 归档
状态: 已过期
归档日期: 2026-07-26
原始时间: 2026-07（源码拆分重构期）
---

# 重构质量跟进交接（2026-07）

> **已过期，仅供追溯。**
> 文中的 `src/core/`、`src/graphql/` 等多级目录路径对应当时的源码结构，后续已合并为 `src/` 下的 9 个功能域文件。
> 当前的构建与源码约定请看[开发指南](../指南/开发指南.md)。

记录 userscript 源码拆分与构建重构后的发布就绪跟进。

## 1. 本轮已完成

- 恢复 userscript metadata 版本为 `0.3.4`（`src/userscript-header.js`），并重新生成 `LinuxDo Hub Tool.user.js`。
- 从 `SOURCE_FILES` 移除纯注释的兼容占位文件 `src/core/state-and-queries.js` 并删除该文件。
- 保持 `src/channels/channel-cache.js` 已删除；渠道缓存修改逻辑保留在 `src/core/channel-cache.js`。
- 在 `scripts/build-userscript.js` 中新增关键 `SOURCE_FILES` 顺序守卫：
  - `src/core/constants.js` 在 `src/core/state.js` 之前
  - `src/core/state.js` 在 `src/core/channel-cache.js` 之前
  - `src/graphql/pricing-fields.js` 在 `src/core/fetch-patch.js` 之前
  - `src/core/bootstrap-and-test-exports.js` 保持最后

## 2. 修复后的验证记录

重建后以下检查全部通过（4 个测试文件通过）：

```bash
node scripts/build-userscript.js
node scripts/check-userscript.js
node --test tests/*.test.js
node --check "LinuxDo Hub Tool.user.js"
node --check scripts/build-userscript.js
node --check scripts/check-userscript.js
```

## 3. 浏览器手动冒烟清单

需要在 `https://hub.linux.do/` 的真实登录会话中验证：

- [ ] 资源市场页：全部/免费/付费筛选正常，渠道卡片可用。
- [ ] 模型详情页：GraphQL pricing 字段增补后，provider 价格数据正常加载。
- [ ] API 密钥页：弹窗开关、profile 绑定、渠道列表编辑与保存行为正常。
- [ ] 请求页：入口挂载、请求触发控件、渠道映射显示正常。

发布前记录浏览器、账号状态和任何 console/network 报错。

## 4. 建议的后续任务

> 这部分中仍然有效的条目已迁移到[待办事项](../指南/待办事项.md)，以下保留原文用于追溯。

1. 为 `src/graphql/pricing-fields.js` 添加针对性测试：
   - 已有 `mode` 但缺少 `flatFee`
   - 已有 `flatFee` 但缺少 `mode`
   - 多个 `pricing` selection
   - 嵌套的 pricing 形状块
   - fragment 形式的 marketplace model query
2. 补充 fetch body 回归覆盖：
   - `fetch(url, { body })`
   - `fetch(new Request(url, { body }))`
   - pricing 字段注入保持 method、headers、body 不变
   - patched fetch 处理后不出现 body 已消费错误
3. 继续以小步、行为不变的方式拆分 `src/core/fetch-patch.js`：
   - request/header 上下文 helpers
   - response 包装 helpers
   - marketplace 渠道 URL 清理 helpers
4. 在准备 ESM/esbuild 期间保持当前零依赖拼接构建为发布基线：
   - 源码边界稳定后再引入显式 import/export
   - 先新增并行 bundler 输出
   - bundler 输出通过相同的发布检查和浏览器冒烟前，不替换 `scripts/build-userscript.js`
